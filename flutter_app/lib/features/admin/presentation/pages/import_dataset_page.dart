import 'package:flutter/material.dart';

class ImportDatasetPage extends StatelessWidget {
  const ImportDatasetPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'Import Dataset - Coming Soon',
          style: TextStyle(
              fontSize: 22, fontWeight: FontWeight.bold, color: Colors.grey),
        ),
      ),
    );
  }
}
